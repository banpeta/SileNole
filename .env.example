# Copia este archivo a .env.local y rellena los valores.
# .env.local esta ignorado por git (*.local) y contiene las claves reales.
#
# Fase 7 (sincronizacion multi-dispositivo, ADR-004/008/009).
# Usa SIEMPRE la clave publishable/anon, nunca la service_role.
VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
